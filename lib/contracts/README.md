# API contracts

Zod schemas in this folder are the **canonical** request/response contracts for public `app/api` routes.

- TypeScript imports: `@/lib/contracts` or specific modules.
- OpenAPI: hand-maintained mirror at [`public/openapi.yaml`](../../public/openapi.yaml) (keep in sync when changing shapes).
